{
  "name": "@local/library-membership",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bounded context: who holds a library card, what they are entitled to, and whether they are in good standing.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@local/ddd-core": "workspace:*",
    "@local/event-bus": "workspace:*",
    "@local/shared-kernel": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b"
  }
}
