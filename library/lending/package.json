{
  "name": "@local/library-lending",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bounded context: loans and the queue of members waiting for a title. Talks to the other contexts only through ports it owns.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@local/ddd-core": "workspace:*",
    "@local/event-bus": "workspace:*",
    "@local/shared-kernel": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b"
  }
}
