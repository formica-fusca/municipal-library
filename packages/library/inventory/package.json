{
  "name": "@local/library-inventory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bounded context: the physical volumes on the shelves. The showcase aggregate of this repository.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@local/ddd-core": "workspace:*",
    "@local/event-bus": "workspace:*",
    "@local/shared-kernel": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b"
  }
}
